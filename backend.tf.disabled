terraform {
  backend "s3" {
    bucket = "cloudzone100"
    key    = "terraform-state/project/terraform_state"
    workspace_key_prefix = "prefix"
    region = "us-east-1"
  }
}

#dev/terraform_dev.tfstate